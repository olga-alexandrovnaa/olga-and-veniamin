# Переменные окружения не нужны.
# Гости подгружаются из таблицы по коду в ссылке (таблица должна быть доступна «по ссылке»).
# Чтобы сохранять в таблицу подтверждения и опрос — разверните Apps Script и укажите URL в src/config/survey.ts → APP_SCRIPT_URL.
